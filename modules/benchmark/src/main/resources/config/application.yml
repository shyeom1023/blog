server:
  port: 10020
  shutdown: graceful
management:
  server:
    port: 10021
spring:
  profiles:
    include:
    group:
      local:

    active: local
  application:
    name: benchmark
  lifecycle:
    timeout-per-shutdown-phase: 15s
#  cloud:
#    stream:
#      function:
#        autodetect: false