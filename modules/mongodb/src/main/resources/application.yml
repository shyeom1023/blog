spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/test

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
    org.springframework.data.mongodb.repository.query.MongoQueryMethod: DEBUG
    com.mongodb.Mongo: DEBUG
